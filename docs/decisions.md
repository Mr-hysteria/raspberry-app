# Decisions Log

> 用途：记录关键技术决策，重点写清楚“背景、选择、原因、影响”。

## 记录规则

- 只有在出现重要取舍时才新增，不需要为所有小改动都写一条
- 一条记录应回答下面几个问题：
  - 当时遇到了什么问题
  - 备选方案有哪些
  - 最终选了什么
  - 为什么这样选
  - 带来了什么收益和代价

---

## DEC-001 从 `HTML + Chromium` 切换到 `Rust + Slint`

### 状态

- 已采纳

### 日期

- 2026-04-20

### 背景

- 目标设备是 `Raspberry Pi Zero 2 W`，只有 `512MB` 内存
- 原有 `HTML + Chromium` 方案实现快，但运行时成本较高，不利于长期稳定运行

### 备选方案

- 保持 `HTML + Chromium`
- 切换到其他原生 GUI 路线
- 切换到 `Rust + Slint`

### 最终决策

- 项目切换到 `Rust + Slint` 原生实现，并使用软件渲染方式运行

### 原因

- 降低内存占用
- 移除浏览器运行时依赖
- 减少首次运行弹窗和桌面浏览器副作用
- 更适合后续做开机直启和长期稳定运行

### 影响

- 代码结构从网页资源转为 Rust 源码 + Slint UI
- 运行方式从浏览器启动转为本地二进制启动
- 部署重心转移到 Rust 构建、交叉编译和树莓派运行脚本

### 代价与注意事项

- 团队需要维护 Rust / Slint 构建链路
- UI 调整需要重新编译，不像前端页面那样即改即看
- 后续每次 UI 或运行链路调整，都应同步维护文档资产

---

## 模板

复制下面结构新增下一条即可：

```md
## DEC-XXX 决策标题

### 状态

- 已采纳 / 已废弃 / 待确认

### 日期

- YYYY-MM-DD

### 背景

- 问题背景

### 备选方案

- 方案 A
- 方案 B
- 方案 C

### 最终决策

- 最终采用的方案

### 原因

- 为什么这样选

### 影响

- 对代码、部署、协作、性能、成本有什么影响

### 代价与注意事项

- 需要承担的成本
- 后续维护时需要注意的点
```
