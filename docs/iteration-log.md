# Iteration Log

> 用途：记录每次迭代做了什么、为什么做、影响了什么，以及下一位接手者需要注意什么。

## 使用规则

- 每次迭代结束后至少新增一条记录
- 记录重点放在：
  - 本次目标
  - 实际完成项
  - 影响范围
  - 遗留问题
  - 下一步建议
- 不要求写很长，但要让下一位 agent 能在 1 分钟内理解上下文

---

## 2026-06-25 - 异常退出恢复与字号优化

### 目标

- 修复系统内存不足杀死时钟后停留桌面的问题
- 提升小屏远距离阅读时的文字可见度

### 完成项

- 确认退出根因是系统 `PK-Backend` 触发内存压力后 OOM Killer 杀死时钟
- 新增轻量守护脚本，时钟退出后 3 秒自动重启
- 自启动和部署流程统一通过守护脚本启动
- 放大年度/CPA 卡片标题、考试日期和中文金句

### 影响范围

- `scripts/watch-clock.sh`
- `scripts/install-autostart.sh`
- `scripts/deploy-and-run-pi.sh`
- `ui/clock.slint`

---

## 2026-06-25 - 开机自动启动

### 目标

- 树莓派重启并自动登录桌面后，无需 SSH 即可启动时钟

### 完成项

- 新增 LXDE 标准桌面自启动安装脚本
- 初始化脚本完成构建后自动安装自启动项
- 部署脚本每次部署时同步并修复自启动项
- 新增自启动配置生成测试

### 影响范围

- `scripts/install-autostart.sh`
- `scripts/bootstrap-pi.sh`
- `scripts/deploy-and-run-pi.sh`
- `tests/install-autostart.sh`

---

## 2026-06-25 - 常亮与每日刷新修复

### 目标

- 修复白天空闲约 10 分钟后自动息屏
- 将每日一句刷新改为按本地自然日判断

### 完成项

- 将 X11 DPMS 自动超时设置为 `0/0/0`，只允许应用主动息屏
- 应用启动后首次同步会明确执行一次亮屏或息屏
- 每日一句以返回的 `dateline` 和本地日期比较
- 当天更新失败或接口尚未更新时每 15 分钟重试

### 影响范围

- `run-clock.sh`
- `src/main.rs`
- `src/daily_quote.rs`
- `src/display_power.rs`
- `tests/run-clock-config.sh`

---

## 2026-06-24 - 专注时钟精简改版

### 目标

- 删除日节律树和阅读模式，突出关键时间与考试目标

### 完成项

- 采用沉浸式海报布局
- 新增年度剩余百分比和 CPA 8 月 29 日倒计时
- 接入金山词霸双语每日一句、背景图和本地缓存
- 新增 23:30–07:00 息屏及夜间触摸唤醒 60 秒
- 为日期、倒计时、接口解析和屏幕状态新增单元测试

### 影响范围

- `src/main.rs`
- `src/domain.rs`
- `src/daily_quote.rs`
- `src/display_power.rs`
- `ui/clock.slint`
- 产品与维护文档

### 遗留项

- 需要在真机确认触摸控制器能否在 DPMS 息屏后把事件传递给应用
- 每日一句背景图裁切效果会随上游图片构图变化

### 下一步建议

- 观察一周内每日一句缓存和夜间唤醒的稳定性
- 如触摸事件被 DPMS 拦截，再增加 Linux 输入设备监听

---

## 2026-04-20

### 目标

- 为项目建立一套可持续维护的 agent 文档资产

### 完成项

- 新增 `agent.md` 作为 agent 协作入口
- 新增 `docs/iteration-log.md` 作为迭代沉淀模板
- 新增 `docs/decisions.md` 作为关键技术决策记录模板
- 新增 `docs/prd-healing-life.md`，沉淀“愈见·生活”方向的产品需求文档 v2
- 更新 `README.md`，加入文档入口说明

### 影响范围

- 文档结构
- 后续协作流程
- 迭代收尾规范
- 产品需求记录方式

### 遗留项

- 还没有独立的 `architecture` 文档
- `开发SOP.md` 仍含环境专属敏感信息，后续应脱敏

### 下一步建议

- 下一次涉及架构或部署方式变更时，补充决策记录
- 后续如功能逐步增多，再新增 `docs/architecture.md` 或 `docs/domain.md`

---

## 2026-04-20 - Healing Life MVP 开发

### 目标

- 将“愈见·生活”PRD v2 落地为第一版可运行的界面与交互

### 完成项

- 重构 `ui/clock.slint` 为疗愈型节律布局
- 重写 `src/main.rs` 状态逻辑，加入：
  - 日进度与年进度计算
  - 本地语录按时段切换
  - 轻触切换语录
  - 长按进入阅读模式
  - 阅读倒计时与退出逻辑
  - 昼夜主题切换
- 更新 `README.md` 当前特性描述
- 更新 `agent.md` 当前状态描述
- 补充 `开发SOP.md` 中的 SSH 连接与远程运行手册
- 新增 `scripts/deploy-and-run-pi.sh`，默认使用本机 SSH 别名 `raspberry-clock`
- 通过 `cargo build` 构建验证

### 影响范围

- `src/main.rs`
- `ui/clock.slint`
- `README.md`
- `agent.md`

### 遗留项

- 还没有真机上的视觉截图与触摸实测结果
- 当前背景呼吸感较轻，后续可继续细化但不宜过重
- 阅读模式时长目前固定为 30 分钟，尚未抽为配置项

### 下一步建议

- 在树莓派真机上验证触摸、字体和布局表现
- 如视觉方向确认稳定，再细化植物感日进度表现
- 后续补一份 `docs/architecture.md`，记录状态流和交互流

---

## 模板

复制下面结构新增下一条即可：

```md
## YYYY-MM-DD

### 目标

- 本次迭代目标

### 完成项

- 完成了什么

### 影响范围

- 影响到哪些模块、脚本、文档、运行方式

### 遗留项

- 暂未解决的问题
- 需要后续确认的风险

### 下一步建议

- 建议下一位 agent 或开发者继续做什么
```
